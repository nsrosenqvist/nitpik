{
  "description": "Refactor from a context manager to manual open/close introduces a file-handle leak: the early `return \"\"` on an empty file skips `f.close()`.",
  "kind": "positive",
  "profiles": ["backend"],
  "expected": [
    {
      "file": "reader.py",
      "line": 2,
      "end_line": 6,
      "min_severity": "warning",
      "keywords": ["leak", "close", "handle", "resource", "context manager", "not closed"],
      "note": "the leak spans open() on line 2 through the skipped f.close() on line 6 (early return on line 5); a finding anchored anywhere in that span counts"
    }
  ]
}
