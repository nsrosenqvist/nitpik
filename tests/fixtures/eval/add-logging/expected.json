{
  "description": "Adds a benign log line (an item count, no sensitive data) plus the import it needs. No behavior change; should draw no warning/error finding.",
  "kind": "negative",
  "profiles": ["backend"],
  "expected": []
}
